# Vant H5 面试题库

> **怎么用：** 先盖住答案，自己口述 1～2 分钟（适配、弱网、WebView 坑），再点开 `参考答案` 对照补洞。追问用来模拟面试官加压；踩坑是实战里最容易翻车的点。中英文术语之间请习惯留空格，口述时也建议如此。

本模块偏「十年经验口述」：少背 Vant 组件 props，多讲**真机差异、安全区、键盘、弱网与容器（WebView）**。

---

### Q1. H5 适配你会选 rem、vw 还是别的？viewport 怎么讲清楚？

**考察点：** viewport、rem / vw / 响应式、一倍稿与 DPR

::: details 参考答案
先定目标：设计稿通常 375 / 750，要在不同手机宽度上「比例正确」，并处理大屏不要无限放大。

常见方案：
1. **vw**：CSS 直接按设计稿换算，链路短；注意最小 / 最大字号与横屏；
2. **rem**：`html` 根字号随宽度变（flexible 思路或 postcss-pxtorem），组件内用 rem；灵活，但要搞清「哪些用 rem、哪些用 px」（1px 边框、字号下限）；
3. **现代响应式**：布局用 flex / grid + 断点，间距用 clamp；适合内容型，不只是「整页缩放」。

viewport：`width=device-width, initial-scale=1, viewport-fit=cover`（全面屏安全区时常要 cover）。口述别背公式，要讲**你怎么保证视觉稿还原、大屏是否限宽、与 Vant 的 375 约定如何对齐**。Vant 本身按 375，改根字号要确认没把组件库字号带飞。
:::

**追问链：**
1. 为什么有人禁止 `user-scalable=no`？无障碍与业务防误触怎么权衡？
2. `1px` 细线在 DPR=3 设备怎么处理？
3. 横屏、折叠屏你怎么降级布局？

::: details 追问参考答案

**1. 为什么有人禁止 `user-scalable=no`？无障碍与业务防误触怎么权衡？**

不应为防误触全局禁止缩放，因为低视力用户依赖双指缩放，WCAG 也要求内容可放大。误触应通过足够大的点击区域、操作间距、提交确认和防重复机制治理，而不是牺牲可访问性。若支付画布等局部场景确有手势冲突，应只限制局部手势并提供等价操作，再用系统字体放大和读屏真机验证。

**2. `1px` 细线在 DPR=3 设备怎么处理？**

CSS 的 `1px` 是一个 CSS 像素，在 DPR=3 时通常由约 3 个物理像素表达，并不等于设计稿所说的单物理像素。普通分隔线直接用 `1px` 最稳定；若视觉必须更细，可用伪元素绘制后按 DPR 缩放，或使用渐变，但要检查缩放、圆角和子像素取整。不要用 UA 硬编码机型，真机截图对比更可靠。

**3. 横屏、折叠屏你怎么降级布局？**

横屏和折叠屏不能只按宽度等比放大。我会用媒体查询、容器查询和 `clamp()` 设最大内容宽度，横屏时把底部操作改为侧边或紧凑布局；折叠设备再按能力检测铰链分区，无法识别时退化为居中单栏。监听方向与可视区域变化，并覆盖旋转、分屏、折叠展开和字体放大的真机用例。

:::

**踩坑：** 只改业务页 rem，第三方嵌入页或弹层按 px 写死，一放大全错位——适配策略要全应用一致。

---

### Q2. 安全区（safe-area）在刘海屏 / 底部横条上你怎么落地？

**考察点：** `viewport-fit`、`env(safe-area-inset-*)`、固定底栏、Vant 组件

::: details 参考答案
全面屏：顶部刘海 / 灵动岛、底部 Home Indicator 会吃掉固定头尾。做法：
1. `viewport-fit=cover` 允许铺满；
2. 用 `env(safe-area-inset-top/bottom/...)`（或 `constant()` 兼容旧 iOS）给固定栏加 padding；
3. Vant 的 `Tabbar`、`NavBar`、弹层类组件关注其 safe-area 相关 props / 样式变量，别重复加双倍 padding。

内容区：底部固定按钮要抬高；全屏弹层的关闭按钮别贴物理边缘。Android 厂商差异大，真机抽检比只看模拟器重要。

口述结构：为什么需要 → CSS 环境变量 → 和组件库如何配合 → 真机验收清单。
:::

**追问链：**
1. 横屏时 inset 变化，固定样式会出什么问题？
2. 内嵌 App WebView 时安全区以谁为准——网页还是原生导航？
3. `safe-area` 与键盘顶起同时出现，优先级怎么处理？

::: details 追问参考答案

**1. 横屏时 inset 变化，固定样式会出什么问题？**

横屏后刘海可能转到左侧或右侧，四个 `safe-area-inset-*` 会重新计算。若只写死竖屏的顶部和底部 padding，导航按钮可能被遮住，固定底栏也可能横向贴边。应使用四向 `env()` 与逻辑布局，在方向变化后让 CSS 自动重算；全屏弹层、左右手持方向和旋转动画期间都要真机检查。

**2. 内嵌 App WebView 时安全区以谁为准——网页还是原生导航？**

以双方约定的唯一责任边界为准，而不是固定认定网页或原生。若原生已把 WebView 放在安全区域内，H5 不应再消费同一 inset；若 WebView 全屏铺到物理边缘，则由 H5 使用 `env()`，或由原生通过 Bridge/CSS 变量提供可信值。协议需说明顶部、底部和沉浸式模式，并按 App 版本做兼容与真机验收。

**3. `safe-area` 与键盘顶起同时出现，优先级怎么处理？**

键盘出现时先保证输入焦点和操作区位于视觉视口内，安全区只处理仍暴露的物理边缘，不能把键盘高度与 bottom inset 机械相加。可根据 `visualViewport` 的高度、offset 与 focus 状态切换键盘布局，隐藏或改流式排列固定底栏；键盘关闭后再恢复安全区。需验证 iOS、Android 和不同 WebView 的 resize 策略。

:::

**踩坑：** 给 `body` 加了 bottom inset，又给 `van-tabbar` 加一次，底部巨空白——先分清「谁负责吃安全区」。

---

### Q3. 滚动穿透（遮罩底下页面跟着滚）你怎么根治？

**考察点：** touch 事件、body 锁滚、`overflow`、iOS 弹性滚动

::: details 参考答案
典型场景：Popup / Dialog / ActionSheet 打开时，底下列表仍在滚。根因是触摸滚动默认作用到背后滚动容器，iOS 弹性滚动更明显。

治理组合拳：
1. 打开弹层时锁 `body`（`overflow: hidden` + 记下 scrollTop，关时还原）——注意会丢掉滚动位置若没记；
2. 弹层内容区自己滚，并在边界 `touchmove` 上 `preventDefault`（要 `passive: false`，且只在需要时绑）；
3. 优先用 Vant 已处理穿透的组件能力，别层层自造；
4. 新方案可关注 `overscroll-behavior: contain`。

十年经验：锁 body 会影响 `position: fixed` 元素抖动——要统一滚动容器策略（只让 `#app` 内某层滚）。
:::

**追问链：**
1. 为什么有的页面锁 body 后弹层里也滚不动？
2. 多弹层叠加开关顺序乱了，滚动锁怎么引用计数？
3. 和「整页是自定义下拉刷新容器」冲突时怎么排？

::: details 追问参考答案

**1. 为什么有的页面锁 body 后弹层里也滚不动？**

常见原因是弹层仍处于被锁的滚动链中，或全局 `touchmove` 监听无差别 `preventDefault`，导致内部可滚区域也收不到默认滚动。应让弹层内容成为独立滚动容器，设置明确高度和 `overflow: auto`，仅在到达边界且会穿透时阻止事件；同时检查监听器是否以 `passive: false` 注册，避免误以为阻止已生效。

**2. 多弹层叠加开关顺序乱了，滚动锁怎么引用计数？**

滚动锁应由统一管理器按锁实例或 token 记录，而不是每个弹层各自改 body。首次加锁时保存原样式和 scrollTop；每次打开登记 token，关闭只删除自己的 token，集合为空才恢复样式与位置。组件卸载、路由离开和异常关闭都执行幂等释放，并对重复释放告警，这样关闭下层弹层不会提前解锁上层。

**3. 和「整页是自定义下拉刷新容器」冲突时怎么排？**

先统一谁拥有纵向手势：页面滚动时由下拉刷新容器处理，弹层打开后则暂停页面刷新，只允许弹层内容滚动。手势开始时根据起点、方向阈值和容器是否位于顶部决定归属，确认后不要中途抢占；可配合 `touch-action`、`overscroll-behavior` 和非被动监听。重点真机验证斜滑、边界回弹与快速连续开关。

:::

**踩坑：** 全局 `touchmove.preventDefault`——把弹层内部滚动一并掐死，用户以为页面坏了。

---

### Q4. 弱网环境下 H5 体验你怎么做「能用」而不是「能演示」？

**考察点：** 超时、重试、骨架屏、降级、缓存与离线

::: details 参考答案
弱网不是加个 loading 那么简单。分层：
1. **预期**：骨架屏 / 占位，避免白屏；关键操作按钮防重复提交；
2. **请求**：合理超时、幂等重试（只对安全的 GET / 明确幂等接口）、失败文案分「无网 / 超时 / 业务错」；
3. **数据**：列表保留旧数据再静默刷新；静态资源长缓存；接口层可考虑 SWR 思路；
4. **降级**：大图降清晰度、自动播放视频改点击播放、非首屏模块延迟请求；
5. **可观测**：把慢请求、失败率按页面打点，别等客服反馈。

口述要带场景：地铁、电梯、活动高峰——你怎么保证下单 / 支付路径最短、少依赖非关键资源。
:::

**追问链：**
1. 重试会不会造成「下单两次」？边界怎么定？
2. Service Worker 在活动 H5 里你敢上到什么程度？
3. 接口 3 秒才回，产品要「秒开」，你怎么谈判拆首屏数据？

::: details 追问参考答案

**1. 重试会不会造成「下单两次」？边界怎么定？**

会，因此网络超时不代表服务端未执行。GET 等安全请求可退避重试；下单等写请求只有在服务端支持幂等键、去重窗口和结果查询时才能重发，并复用同一个业务幂等键。客户端把超时记为 unknown，先按订单号查状态，再决定提示或续办；支付、扣款等高风险动作不应由通用拦截器自动重试。

**2. Service Worker 在活动 H5 里你敢上到什么程度？**

我只把它用于版本化静态资源和明确可公开缓存的 GET，并采用可回滚的更新策略；HTML 通常 Network First 或短缓存，避免活动规则发布后仍命中旧壳。离线队列只保存最小非敏感载荷与幂等键，不持久化 Token、支付材料或身份信息，注销即清理。还要接受部分 WebView 不支持或限制 Service Worker，始终保留在线降级路径。

**3. 接口 3 秒才回，产品要「秒开」，你怎么谈判拆首屏数据？**

先用瀑布图和真实用户数据证明 3 秒花在服务端、网络还是串行依赖，再把「秒开」定义为可见核心内容而非所有数据完成。与产品按价值拆出首屏最小接口，价格和主状态优先，推荐、评价等延后并行；可展示带时间戳的旧数据再刷新，但交易字段不能伪缓存。最终用 LCP 和业务可用点共同验收。

:::

**踩坑：** loading 遮罩全局锁死 30 秒——用户杀进程重进，本地状态与服务端不一致。

---

### Q5. 移动端输入框与软键盘，你踩过哪些坑？怎么处理？

**考察点：** 键盘顶起、`visualViewport`、focus 滚动、iOS 固定底栏

::: details 参考答案
高频问题：
1. **固定底栏被顶飞 / 遮挡**：iOS 上 `position: fixed` 随键盘表现不一致；应用 `visualViewport` 监听或改为非固定布局；
2. **聚焦后页面被顶起，收起键盘留下空白**：滚动复位；注意 `scrollIntoView` 别过猛；
3. **安卓 WebView 高度变化**：`window.resize` / `visualViewport.resize` 更新布局；
4. **输入体验**：`type`、`inputmode`、`pattern` 正确；数字键盘与小数；禁用自动缩放（字号 ≥16px 是老套路之一）；
5. **Vant Field** 与表单：错误提示别被键盘挡住，提交时滚到首个错误字段。

口述金句：键盘问题没有统一 CSS 银弹，要**分 iOS / Android / 内嵌 WebView** 验收。
:::

**追问链：**
1. 为什么有的机型 `100vh` 含键盘区域，布局会跳？
2. 弹层内输入框聚焦，滚动容器该滚弹层还是 window？
3. 第三方安全键盘 / 一键登录 SDK 插入时如何共存？

::: details 追问参考答案

**1. 为什么有的机型 `100vh` 含键盘区域，布局会跳？**

默认 `interactive-widget=resizes-visual` 时，软键盘通常只缩小 VisualViewport，布局视口以及 `dvh`、`svh`、`lvh` 并不随键盘变化；只有内核支持并采用 `resizes-content` 时，布局视口和动态视口单位才可能跟随。键盘布局应以 `visualViewport.height/offsetTop` 和焦点状态为主，视口单位处理地址栏等变化，旧 WebView 再用流式布局降级。

**2. 弹层内输入框聚焦，滚动容器该滚弹层还是 window？**

应滚动实际承载该字段的弹层内容容器，window 和背景保持锁定，否则会出现弹层不动、底页位移或关闭后位置丢失。聚焦或校验失败时计算字段相对弹层可视区的位置，仅做最小滚动并预留键盘与操作栏空间；不要每次 input 都调用 `scrollIntoView`。关闭时恢复焦点和原页面 scrollTop，并验证嵌套滚动场景。

**3. 第三方安全键盘 / 一键登录 SDK 插入时如何共存？**

把第三方 SDK 当作会改变可视区域和焦点规则的原生能力，通过 Bridge 约定 ready、shown、hidden、height 和取消事件，而不是猜 DOM resize。展示期间暂停 H5 自己的键盘位移与滚动修复，只保留安全区责任边界；回调需超时、幂等且支持旧版本无事件时降级。Token、手机号等敏感结果只走受控通道，不写 URL、日志或长期缓存。

:::

**踩坑：** 用 JS 强行 `window.scrollTo(0,0)` 清空白，却把用户正在看的长表单滚跑——要只在 keyboard dismiss 时针对性修。

---

### Q6. H5 路由返回栈与「物理返回 / 右滑返回」你怎么设计？

**考察点：** history、栈管理、拦截返回、与 App 容器协作

::: details 参考答案
浏览器 / WebView 都有历史栈。SPA 用 Vue Router 的 `push/replace/back`。原则：
- 能 `replace` 的中间态（登录后回跳、向导步）别无限 `push`，否则用户按返回会穿梭「脏历史」；
- 弹层 / 筛选面板打开时，可用 `pushState` 占一层，物理返回先关层再退页——体验接近原生；
- 离开未保存表单：`onBeforeRouteLeave` + 确认；
- 内嵌 App：有的容器会劫持返回，需与 JSBridge「返回消费」协议对齐（前端先处理，处理不了再交给原生）。

口述讲清：**返回是产品逻辑，不是只会 `router.back()`**。
:::

**追问链：**
1. 微信支付 / 授权中间页跳回，栈乱了怎么修？
2. `keep-alive` 列表页返回保留位置，和路由缓存怎么配？
3. 同一 URL 多打开几次，返回路径爆炸如何治理？

::: details 追问参考答案

**1. 微信支付 / 授权中间页跳回，栈乱了怎么修？**

不要依赖固定 `back(-2)` 修栈。发起前生成随机一次性 OAuth state 并绑定服务端会话，适用时配合 PKCE；returnUrl 优先由服务端保存，否则只接受严格同源白名单，防止开放重定向。回调校验并消费 state 后用 `replace` 回目标页，再按业务单号查单恢复；支付结果以服务端为准，回跳参数不能证明成功。

**2. `keep-alive` 列表页返回保留位置，和路由缓存怎么配？**

缓存应绑定稳定路由 key、筛选条件和数据版本，而不是只记一个 scrollTop。离开时保存滚动锚点与查询快照，返回激活后先恢复列表 DOM，再定位 itemId 或位置；若缓存过期、用户切换或筛选变化则重新请求并谨慎恢复。详情更新了列表项时做局部合并，避免为了保位置长期展示陈旧价格或状态。

**3. 同一 URL 多打开几次，返回路径爆炸如何治理？**

先区分业务实体页与临时 UI：同一实体重复导航可用 `replace` 或检测当前规范化 URL 后忽略，筛选、弹层等临时状态由统一 overlay 栈管理。历史项附 navigationId 和来源，返回时只消费栈顶一次；登录、重定向等中间态统一 replace。不要粗暴清空浏览器历史，分享直达和外部入口需要明确的首页兜底。

:::

**踩坑：** 每开一个 Popup 都 `push` 历史却忘记关时 `back`，用户要连按多次才能离开页面。

---

### Q7. 埋点 / 数据分析在 H5 你怎么做才不拖垮业务？

**考察点：** 曝光 / 点击、PV/UV、渠道参数、隐私合规

::: details 参考答案
分层：
1. **自动**：PV、路由变化、基础性能（可接 GA / 神策 / 自研）；
2. **业务**：关键漏斗（曝光商品、点购买、提交订单）——事件名与属性字典化，禁止各写各的中文描述；
3. **技术**：接口失败、白屏、资源超时——给排查用。

实现：异步上报、批量、失败重试；别在点击主路径同步 `await` 埋点。SPM / 渠道：`utm` 或业务 trace 从落地页透传到下单。合规：隐私协议前少采个人信息；可识别信息脱敏。

十年经验：埋点挂了业务不能挂；埋点质量靠评审与校验平台，不靠「多打点」。
:::

**追问链：**
1. 曝光埋点如何防「滚过去就算」的误报？
2. SPA 路由 PV 与浏览器刷新如何去重？
3. 和 App 原生埋点如何打通同一用户路径？

::: details 追问参考答案

**1. 曝光埋点如何防「滚过去就算」的误报？**

曝光不能只以元素进入视口为准。我会用 IntersectionObserver 同时要求可见比例和连续停留时长，页面后台、遮罩覆盖或快速滚过时取消计时；按 itemId、坑位和本次页面会话生成 eventId 去重。虚拟列表节点复用时状态必须跟业务 ID 走，不能跟 DOM 走，并抽样回放曝光漏斗校验阈值是否造成漏报或虚高。

**2. SPA 路由 PV 与浏览器刷新如何去重？**

刷新是新 PV，同一次导航被多处观察才去重。`PerformanceNavigationTiming.type` 只描述文档级 navigate、reload、back_forward，不能区分 SPA 的 afterEach。SPA 导航由 Router 生成 navigationId；返回结合 Router、`popstate` 和可用的 Navigation API，Soft Navigation 仅渐进增强。重定向只记最终页。

**3. 和 App 原生埋点如何打通同一用户路径？**

由原生生成 sessionId/traceId，经可信 Bridge 注入，H5 的 PV、点击和请求继续携带；双方各自用本进程单调时钟记录阶段耗时，因为 Native 与 WebView 没有天然共享的单调时钟。通过导航边界握手或双端都观测到的事件校准时间轴，再关联 navigationId/eventId。离线补报保留原发生时间，且关联字段不得包含 Token、手机号等敏感值。

:::

**踩坑：** 在 `computed` / 模板渲染路径里发请求打点——一卡顿或重复渲染就数据爆炸。

---

### Q8. H5 首屏优化你怎么抓？和「整站性能」有何不同？

**考察点：** 关键渲染路径、资源优先级、接口并行、Vant 按需

::: details 参考答案
首屏目标：最快看到**有效内容**（不只是壳）。抓手：
1. **减请求**：路由懒加载、Vant 按需、首屏无关模块后置；
2. **减阻塞**：关键 CSS 内联慎重、字体子集、图片尺寸与格式（WebP/AVIF）、骨架代替白屏；
3. **数据**：首屏接口并行；能内嵌首屏数据更好（注意缓存与个性化）；
4. **容器**：CDN、HTTP/2、DNS 预解析；WebView 可考虑预热与资源预置（与端上协作）；
5. **度量**：LCP / FCP（或业务「首屏可交互」自定义点），按机型与网络分桶。

口述别堆手段：先讲**你如何定义首屏完成**，再讲最大瓶颈是包体、接口还是图片。
:::

**追问链：**
1. 活动页首屏要「秒开」，框架 + UI 库体积怎么砍？
2. 骨架屏和真实布局不一致造成的 CLS 你怎么处理？
3. 慢的是 WebView 初始化还是 H5 本身，如何归因？

::: details 追问参考答案

**1. 活动页首屏要「秒开」，框架 + UI 库体积怎么砍？**

先用构建分析确认首屏实际引入，而不是凭包名猜体积。路由和非首屏模块动态加载，Vant 只按项目当前版本官方推荐方式引入所需组件与样式，避免同时注册整库和重复工具依赖；活动页若交互很少，可只用原生 CSS 实现局部展示。再配合 CDN、压缩和长期缓存，以低端机解析执行时间及 LCP 验收，而非只看 gzip 数字。

**2. 骨架屏和真实布局不一致造成的 CLS 你怎么处理？**

骨架必须复用真实布局的宽高约束，图片用固定尺寸或 `aspect-ratio` 占位，文本按预期行数与行高预留，不能用随意灰块。接口返回后尽量在原槽位替换；顶部插入广告、字体切换和异步组件也要预留空间。用 PerformanceObserver 或 `web-vitals` 定位 layout shift 来源，排除用户主动操作后的位移，再按移动端 P75 观察 CLS。

**3. 慢的是 WebView 初始化还是 H5 本身，如何归因？**

需要原生与 H5 共用 traceId 和单调时间轴。原生记录点击、创建 WebView、内核 ready、开始导航；H5 记录 navigationStart、TTFB、FCP、LCP 与业务可用点，再按 App 版本、机型和网络分桶。若 navigationStart 前耗时高，优先查容器创建与预热；之后才是网络和渲染。Core Web Vitals 本身不覆盖启动前阶段，不能让 LCP 承担全部责任。

:::

**踩坑：** 首屏为了炫把轮播大图 + 视频全上，指标全挂——视觉与性能要产品一起砍。

---

### Q9. H5 与 App WebView 的 JSBridge 你怎么设计与排障？

**考察点：** Bridge 协议、版本兼容、鉴权、调试

::: details 参考答案
Bridge 是「前端调原生能力 / 原生调前端」的契约：登录态、关闭 WebView、分享、支付、扫码、取设备信息等。

设计要点：
1. **协议稳定**：方法名、参数、错误码版本化；未知方法优雅降级；
2. **调用形态**：Promise 化封装 + 超时；原生回调防重复；
3. **注入时机**：`Ready` 事件后再调，别假设 `window.Native` 已在；
4. **安全**：暴露给 H5 的 API 最小集；敏感操作用原生鉴权；
5. **环境探测**：App / 微信 / 浏览器能力矩阵，缺能力走 H5 降级。

排障：约定日志通道、能在 Chrome inspect / Safari Web Inspector 看 H5，原生侧打同 traceId。十年经验：Bridge bug 一半是时序，一半是版本没对齐。
:::

**追问链：**
1. iOS `WKWebView` 与安卓注入差异，前端如何抹平？
2. 登录态放 Cookie、Header 还是 Bridge 注入 Token？
3. 如何防止普通浏览器里恶意页调用你们的伪 Bridge？

::: details 追问参考答案

**1. iOS `WKWebView` 与安卓注入差异，前端如何抹平？**

前端不要直接散落调用平台对象，而应封装统一 Promise RPC：启动先等待 ready，再通过 `getCapabilities` 协商协议版本和方法集；请求带 requestId、超时与至多结算一次语义，页面卸载时拒绝悬空调用。适配层分别处理 iOS 消息通道与 Android 注入对象，能力缺失返回统一错误并走 H5 降级，不能仅靠 UA 判断。

**2. 登录态放 Cookie、Header 还是 Bridge 注入 Token？**

优先选与威胁模型匹配且可撤销的方案：同源业务可用 Secure、HttpOnly、SameSite 合理配置的 Cookie，前端读不到 Token；需要请求头时由受控网络层注入短期凭证。Bridge 可传递一次性授权结果或触发原生代发，但不应把长期 Token 暴露给任意页面 JS。无论哪种都要处理过期、切账号、域名白名单和日志脱敏。

**3. 如何防止普通浏览器里恶意页调用你们的伪 Bridge？**

Android `addJavascriptInterface` 暴露给所有 frame，当前 URL 不能证明调用 frame 来源。新实现优先用 WebMessageListener，以 `allowedOriginRules`、`sourceOrigin`、`isMainFrame` 限制来源。旧 Bridge 必须禁止第三方 frame，配合导航白名单、CSP 和参数校验；支付、取凭证等敏感能力还要基于会话与用户手势做原生二次鉴权。

:::

**踩坑：** 页面 `mounted` 立刻调 Bridge，iOS 偶发未注入——表现为「只有安卓正常」，其实是 Ready 竞态。

---

### Q10. 下拉刷新与列表加载（Vant List / PullRefresh）你怎么用才不出鬼？

**考察点：** 分页、竞态、空态、触底、与滚动容器

::: details 参考答案
模式：`PullRefresh` 管下拉重置；`List` 管触底加载更多。状态机要清晰：`loading`、`finished`、`error`、空列表。

关键坑：
1. **滚动父容器不对**：List 监听的不是那个滚的元素，永远不触底或疯狂触底；
2. **竞态**：快滑时旧页响应覆盖新页——用请求序号或 abort；
3. **重置**：下拉刷新要清空 list、重置 page、`finished=false`；
4. **筛选变化**：等同刷新，别只改参数不重置；
5. **高度**：父级无高度 / `overflow` 不正确会导致「一进页连续加载很多页」。

口述讲状态机比讲组件 API 更像老人。
:::

**追问链：**
1. 总数未知只能「有没有下一页」时，finished 如何判断？
2. 反向列表（聊天）和正向商品列表差异？
3. 下拉刷新与页面自身弹性滚动冲突怎么处理？

::: details 追问参考答案

**1. 总数未知只能「有没有下一页」时，finished 如何判断？**

不要用当前数组长度猜总数，应让接口返回 `hasMore` 或 `nextCursor`。请求成功后先按稳定 ID 去重合并，再以 `hasMore === false` 或游标为空设置 finished；空页但仍给下一游标时继续依据协议，避免提前结束。若后端只能返回 pageSize，可在少于 pageSize 时推断结束，但需承认删除和过滤会误判，并推动接口补充明确分页语义。

**2. 反向列表（聊天）和正向商品列表差异？**

商品列表通常向下追加并保持顶部不动；聊天加载历史是在顶部插入，必须保存插入前的锚点元素及其偏移，渲染后校正 scrollTop，避免视图跳跃。聊天还要处理新消息自动贴底条件、已读线和时间顺序，不能用户看历史时强拉到底。数据用稳定消息 ID 去重，图片异步变高和虚拟化回收也要纳入锚点补偿。

**3. 下拉刷新与页面自身弹性滚动冲突怎么处理？**

只在主滚动容器位于顶部、手势从允许区域开始且纵向下拉超过方向阈值时接管刷新；其余交给浏览器滚动。确认方向后锁定本次手势归属，横向 Swipe、轮播和弹层区域应排除，可结合 `touch-action` 与 `overscroll-behavior`。不要全局阻止 `touchmove`，并在 iOS 回弹、斜滑和嵌套容器上真机调阈值。

:::

**踩坑：** `v-model:loading` 在请求失败时没置回 false——List 卡死，再也加载不了。

---

### Q11. 图片懒加载与占位，在 H5 里你怎么做才兼顾体验和流量？

**考察点：** IntersectionObserver、`loading=lazy`、尺寸占位、CDN

::: details 参考答案
策略：
1. 首屏关键图不懒加载，次屏 / 列表用懒加载（Vant `Lazyload` 或原生 `loading="lazy"` + Observer 兜底）；
2. **占位**：宽高或 aspect-ratio 先占位，防 CLS；骨架或低清 LQIP / 主色块；
3. **体积**：CDN 按屏宽出图、WebP、质量参数；列表缩略图与详情大图分离；
4. **错误**：加载失败占位与重试；
5. **列表复用**：注意组件复用导致图片不刷新——`:key` 与指令更新。

弱网下优先保证「先出字再出图」；别为了动效用超大 APNG 当图标。
:::

**追问链：**
1. 横向滚动列表里懒加载为什么容易「该出不出」？
2. 背景图 `background-image` 如何懒加载？
3. 与 WebP 兼容探测放前端还是 CDN Accept？

::: details 追问参考答案

**1. 横向滚动列表里懒加载为什么容易「该出不出」？**

IntersectionObserver 默认 root 是视口，但交叉计算仍会考虑滚动祖先裁剪；改成横向容器 root 又可能在容器整体离屏时提前预载。先排查容器尺寸、`hidden`、节点复用和调用 `observe` 的时机。预取应使用明确 root，并同时判断容器对页面与图片对容器的双层可见性；兼容内核支持后可评估 `scrollMargin`，旧 WebView 降级为节流矩形计算。

**2. 背景图 `background-image` 如何懒加载？**

背景图没有原生 `loading="lazy"`，可先渲染带宽高占位的元素，用 IntersectionObserver 在接近视口时添加 class 或 CSS 变量，再设置 `background-image`。加载成功后切换状态，失败提供轻量占位和受控重试；首屏关键背景不应懒加载。装饰图还应考虑能否改为普通 `<img>`，以获得响应式图片、优先级和无障碍语义。

**3. 与 WebP 兼容探测放前端还是 CDN Accept？**

优先让 CDN 基于 `Accept` 协商并正确设置 `Vary: Accept`，这样业务代码简单，也能集中升级格式；但必须确认 CDN 缓存键包含格式能力，否则可能把 WebP 发给不支持的客户端。无法可靠协商时，用 `<picture>` 的 `source type` 让浏览器选择 WebP/AVIF 与兜底格式。JS 探测适合作最后降级，不应阻塞首屏或为每张图重复执行。

:::

**踩坑：** 所有图片一张默认大图占位，首屏请求数没少，体积反而暴涨。

---

### Q12. Vant 表单与移动端校验体验，你会怎么做「少烦用户」？

**考察点：** Field 校验时机、错误展示、提交路径、无障碍

::: details 参考答案
移动端表单原则：少打断、错得明白、修得方便。
- 校验时机：输入中别狂弹；失焦或提交时集中校验更常见；
- 错误展示：字段下文案 + 提交时滚动到首错；长表单用步骤条拆开；
- 组件：Picker / Datetime / Area 用 Vant 半屏选择，减少键盘输入错误；
- 主键操作：主按钮固定底部（含安全区），二次提交防抖；
- 无障碍：label 关联、错误用 `aria` 可读（视团队要求）。

和中后台差异：屏幕小，一次只做一件事；能选就不输；能默认就默认。
:::

**追问链：**
1. 服务端校验错误如何映射回具体 Field？
2. 多步骤表单草稿本地缓存如何做与清理？
3. 验证码倒计时与页面缓存 / 返回如何保持？

::: details 追问参考答案

**1. 服务端校验错误如何映射回具体 Field？**

接口应返回稳定字段路径和错误码，例如 `{ field, code, message }`，前端维护字段路径到表单项的映射，把可识别错误写入对应 Field，未知错误显示在表单级区域。提交失败后聚焦并最小滚动到第一个错误，同时让读屏获知错误；不要依赖服务端中文文案解析字段。重新输入时只清理该字段相关错误，跨字段规则则重新校验关联项。

**2. 多步骤表单草稿本地缓存如何做与清理？**

先判断数据敏感度：普通草稿可按用户、业务 ID、schema 版本和 expiresAt 分区保存最小字段；身份证、支付材料、Token 等不进入通用本地缓存，复杂敏感草稿优先服务端保存。本地内容恢复后按不可信输入重新做 schema、权限和业务状态校验。提交成功、过期、注销或切账号立即清理，并提供用户主动删除入口。

**3. 验证码倒计时与页面缓存 / 返回如何保持？**

不要只在内存里每秒减一，否则页面后台定时器被节流、keep-alive 或刷新后都会失真。发送成功时记录服务端返回的可重发时间或本地绝对截止时间，显示值始终由 `deadline - Date.now()` 计算；页面激活时重新计算并恢复计时。最终是否允许重发仍由服务端限流决定，客户端倒计时只是体验约束，不能当安全边界。

:::

**踩坑：** 用 `alert` 做校验失败——系统弹窗体验差，且在 WebView 可能被定制成奇怪 UI。

---

### D1. H5 如何建立 viewport、动态视口单位与组件库的一致适配基线？

::: details 参考答案
#### 基础结论
Q1/Q2 已回答 rem、vw、viewport 与安全区的基础选择；D1 专注浏览器和 App WebView 的动态视口差异、字体缩放、责任边界与跨容器验收。

#### 原理深挖
布局视口、视觉视口和设备像素不是一回事。传统 `100vh` 在地址栏或软键盘变化时可能与可见区域不一致，现代 `svh`、`lvh`、`dvh` 分别表达小、大、动态视口，但内嵌 WebView 的支持取决于系统内核。rem/vw 是 CSS 尺寸映射，不应把 DPR 直接等同于缩放倍数。

#### 工程场景
我会设置标准 viewport，主布局采用 flex/grid 与 `clamp()`，设计稿转换只覆盖约定属性；内容型页面设最大宽度。固定底栏由一个层级消费 `env(safe-area-inset-bottom)`，组件已处理时不重复添加。通过浏览器、iOS/Android WebView、横屏、字体放大和折叠屏矩阵验证。

#### 反例 / 踩坑
全站机械按 750 稿缩放会让平板字号和按钮巨大；禁止用户缩放会伤害无障碍。把 `dvh` 当所有旧 WebView 的通用答案，或 body 与 Vant 组件重复吃安全区，都会产生裁切或巨额空白。

#### 资深回答模板
「60–90 秒主回答：Q1/Q2 定好单位后，我把布局视口、视觉视口和安全区分开治理。响应式布局为主，`dvh`、`visualViewport` 按 WebView 能力渐进增强；原生与 H5 约定谁消费 inset，避免重复。最后用横竖屏、字体放大、iOS/Android 浏览器和 App WebView 真机矩阵验收。」

#### 追问链
1. 用户把系统字体调大后，固定高度组件如何避免截断？
2. iOS 地址栏收起时，`vh`、`dvh` 和 `visualViewport.height` 有何取舍？
3. App 已为 WebView 留出原生安全区，H5 如何协商避免重复？
:::

---

### D2. 滚动容器、软键盘与弹层如何设计成可恢复的状态机？

::: details 参考答案
#### 基础结论
Q3 只回答遮罩滚动穿透，Q5 只回答常见键盘现象；D2 专注多弹层、焦点、视口变化和路由中断组合后的可恢复状态机。

#### 原理深挖
键盘出现时布局视口、视觉视口以及 WebView resize 策略可能不同；`visualViewport` 能提供可见区域变化，但并非所有容器一致支持。body 锁滚常通过记录 scrollTop、固定 body 并在关闭时恢复；多弹层必须引用计数，否则先关闭的弹层会误解锁后面的弹层。

#### 工程场景
封装 `useViewportKeyboard` 与 `useScrollLock`：监听 resize/focus/blur，按阈值和时序判断键盘态，但不把高度变化都武断视为键盘。弹层打开后把初始焦点移到标题、首个字段或主操作，模态场景限制焦点留在弹层内；背景优先使用 `inert`，并以正确的 `aria-modal`、标题关联和兼容降级避免读屏继续访问背景。关闭后把焦点归还触发元素；路由离开和卸载清理监听、样式与锁计数。

#### 反例 / 踩坑
全局 `touchmove.prevent` 会杀死弹层内部滚动；每次 blur 都 `window.scrollTo(0, 0)` 会丢用户位置。只锁滚动却不管理初始焦点、焦点陷阱和背景可访问性，键盘与读屏仍会落到遮罩后；仅用 UA 判断键盘也会在定制 WebView 误判。

#### 资深回答模板
「60–90 秒主回答：在 Q3/Q5 的单点处理之上，我统一主滚动容器，并把锁滚、键盘视口、焦点和路由离开做成状态机。模态弹层设置初始焦点、焦点陷阱与背景 inert/`aria-modal`，关闭后归还焦点；视口优先 `visualViewport` 并降级。所有锁和监听可重入、可清理，再用四类真机验证。」

#### 追问链
1. Popup 内嵌长表单时，错误字段滚动应由谁负责？
2. 原生导航栏高度动态变化，H5 如何获得可信可视区域？
3. 页面缓存后再次激活，旧键盘监听和滚动锁如何防重入？
:::

---

### D3. 弱网与离线恢复如何保证提交不重复、页面不失真？

::: details 参考答案
#### 基础结论
Q4 只回答超时、读请求重试、旧数据展示和弱网降级；D3 专注断网跨进程后的写入恢复、安全载荷、冲突与对账，支付等高风险动作不进入通用离线队列。

#### 原理深挖
GET 可按退避与抖动重试，写请求需服务端幂等键、去重窗口和状态查询。Service Worker 能缓存静态资源和受控的公共 GET，但受注册范围、更新生命周期、存储配额及 WebView 支持影响；含用户敏感数据的接口不应无差别 Cache First。

#### 工程场景
我会建立请求状态机：idle、pending、unknown、success、failure。离线队列只保存完成恢复所需的最小业务载荷和幂等键，**禁止持久化 Token、支付签名/预支付材料、身份证号等敏感数据**；复杂草稿优先存服务端，本地只留草稿 ID、版本和非敏感增量。队列按用户、schema 版本和 expiresAt 隔离，过期、注销或切账号立即清理。恢复出的数据一律视为不可信输入，重新做权限、schema、业务状态与服务端版本校验后才提交。

#### 反例 / 踩坑
Axios 拦截器对所有错误自动重试，会造成重复下单；把 Token、支付材料和整份敏感表单塞进 IndexedDB 会扩大泄漏面。恢复旧草稿后跳过服务端授权与版本检查，也可能覆盖新数据；`navigator.onLine` 仅表示网络接口状态，不保证业务服务可达。

#### 资深回答模板
「60–90 秒主回答：Q4 解决弱网读体验，D3 的离线写只接收明确幂等动作。队列保存最小非敏感载荷，禁止 Token、支付材料和敏感身份数据；服务端草稿优先，本地带用户、版本和过期时间，注销即清理。恢复数据按不可信输入重新鉴权、校验和对账，支付超时只查单不自动重发。」

#### 追问链
1. 支付拉起后 WebView 被杀，回来如何恢复订单状态？
2. 多个离线编辑对同一资源产生冲突，如何提示和合并？
3. WebView 禁用或限制 Service Worker 时如何降级？
:::

---

### D4. WebView Bridge 如何做到协议可演进、调用可追踪且来源可信？

::: details 参考答案
#### 基础结论
Q9 已回答 Promise、ready、能力矩阵与基础排障；D4 专注规模化 RPC 的协议演进、来源信任、敏感能力授权和跨端追踪。

#### 原理深挖
iOS `WKScriptMessageHandler`、Android JavaScript 注入及不同 App 版本的注入时序不同。前端要等待 ready 并维护能力协商，而不是只判断 UA。回调需至多结算一次，页面销毁时拒绝未完成 Promise；原生调用 H5 也应校验目标页面与消息 schema。

#### 工程场景
启动时调用 `getCapabilities` 获取协议版本和方法集；每次请求记录 requestId、H5 release、App 版本与 traceId。高风险方法使用域名 allowlist、短时上下文和原生确认，Token 不通过 query 或日志暴露。协议采用向后兼容字段，废弃方法经历观测、告警和版本门槛。

#### 反例 / 踩坑
仅检查 `window.Native` 存在无法防止恶意页面伪造对象；允许任意 URL 页面调用支付、读取 Token 或打开外部链接会扩大攻击面。没有超时和去重时，原生重复回调会导致重复提交。

#### 资深回答模板
「60–90 秒主回答：在 Q9 的 ready 与 Promise 封装上，我把 Bridge 当版本化 RPC。启动先协商能力，请求有 ID、超时、至多结算一次和统一错误；页面销毁拒绝悬空调用。原生按可信域、会话、用户手势重新授权敏感能力，Token 不进 query 和日志，双端用 traceId 串起发布版本与故障。」

#### 追问链
1. App 新增方法但 H5 先发布，如何优雅降级？
2. 登录 Token 应由 Cookie、请求头还是 Bridge 管理？
3. WebView 跳到第三方域名后，如何撤销 Bridge 暴露？
:::

---

### D5. 浏览器与 App WebView 的返回栈如何组成一致的导航协议？

::: details 参考答案
#### 基础结论
Q6 已回答 `push`/`replace`、基础返回与未保存拦截；D5 专注 SPA、浏览器和 App 三层栈并存时的仲裁、手势取消与支付授权恢复。

#### 原理深挖
`pushState` 只改变浏览器历史，无法天然同步原生手势返回。iOS 侧滑可能先开始原生转场，Android 物理返回则由宿主回调；不同容器还可能禁用 `popstate` 或注入自己的历史。登录、授权、支付中间页应使用可恢复 returnUrl 或业务状态，不依赖「刚好 back 两次」。

#### 工程场景
设计 `canGoBack` / `handleBack` Bridge 协议，H5 返回 `{ consumed, reason }`。Popup 等临时层用统一 overlay stack，只有栈顶消费返回；关键页面保存可恢复业务状态。路由埋点记录 navigationId、来源和动作，自动化用浏览器、安卓物理键、iOS 侧滑及原生关闭按钮分别验收。

#### 反例 / 踩坑
每开弹层都 push 一层、关闭时却忘了回退，会制造幽灵历史；H5 和原生同时执行 back 会直接退出两层。把所有中间跳转都 push，也会让用户在登录和回调页间来回穿梭。

#### 资深回答模板
「60–90 秒主回答：Q6 的单栈规则之上，我把临时 UI、SPA history 和原生容器定义为三层栈。返回按优先级仲裁，并通过 Bridge 明确是否已消费，避免 H5 与原生双退；支付和授权使用可恢复状态、returnUrl 与查单，不依赖固定 back 次数。物理键、侧滑取消和分享直达都进入同一状态机验收。」

#### 追问链
1. 用户从分享链接直接进入二级页，返回应去哪里？
2. `keep-alive` 保留列表位置时，如何避免恢复过期数据？
3. iOS 侧滑进行到一半取消，H5 状态如何保持一致？
:::

---

### D6. H5 埋点如何同时保证数据质量与 Core Web Vitals 可行动？

::: details 参考答案
#### 基础结论
Q7 已回答事件分类和异步上报，Q8 已回答首屏资源优化；D6 专注埋点质量治理与真实用户 Core Web Vitals 诊断。按当前 Core Web Vitals 官方基线，良好阈值是 LCP ≤ 2.5 s、INP ≤ 200 ms、CLS ≤ 0.1；应分别对移动端与桌面端统计第 75 百分位（P75），三项都达到阈值才判定通过。指标与阈值会演进，落地时仍需复核当前官方文档。

#### 原理深挖
LCP 受服务端响应、资源发现、下载和渲染延迟共同影响；INP 观察访问生命周期中的交互延迟；CLS 关注无预期布局位移。WebView 预热可能改变 navigation 起点，iframe 与后台上报也有采集边界，因此需要定义统一口径并记录 App 版本与容器启动阶段。

#### 工程场景
事件 schema 版本化，曝光使用 IntersectionObserver 加可见比例和停留时长，点击用 eventId 去重。性能上采用官方 `web-vitals` 口径或等价实现，按路由、机型、网络、浏览器/WebView 分桶，把 LCP 元素、INP 交互目标和 CLS 来源作为诊断维度。上报异步批量，退出时用可用的可靠通道，但不阻塞业务。

#### 反例 / 踩坑
只报平均值会掩盖长尾；把 FCP 当 LCP、把接口结束当首屏完成，会优化错目标。在 computed、render 或滚动回调直接打点会重复上报并拖慢页面，形成观测干扰。

#### 资深回答模板
「60–90 秒主回答：Q7/Q8 之后，我统一事件 schema 和首屏口径。按当前官方基线，移动端与桌面端分别看 P75：LCP ≤ 2.5 s、INP ≤ 200 ms、CLS ≤ 0.1；同时记录元素、交互目标、位移来源、release、容器和网络用于诊断。事件有去重与质量告警，埋点失败不能阻塞业务。」

#### 追问链
1. WebView 初始化慢与 H5 LCP 慢如何拆分责任？
2. 无 CrUX 数据的内网或 App H5，如何建立性能基线？
3. 埋点被广告拦截或页面快速关闭时，质量如何评估？
:::

---

### D7. 长列表、图片与手势竞争如何在低端机上一起治理？

::: details 参考答案
#### 基础结论
Q10 只回答 Vant List/PullRefresh 状态机，Q11 只回答图片懒加载；D7 专注低端机上列表回收、图片解码、内存峰值与横纵手势竞争的联合诊断。

#### 原理深挖
图片下载完成后仍需解码和上传纹理，大图缩成缩略图显示也会占内存。虚拟列表回收 DOM 时，本地状态和曝光状态必须按 itemId 外置。横向 Swipe 与纵向页面滚动竞争时，应设置方向阈值、touch-action 和被动监听策略，避免每帧同步布局读取后再写样式。

#### 工程场景
Vant List 管分页状态，必要时接入经过真机验证的虚拟方案；请求支持取消和游标去重。图片用 `srcset`/CDN 参数、WebP/AVIF 能力与 aspect-ratio 占位，首屏 LCP 图不懒加载并设置优先级。手势动画用 transform，复杂计算按帧节流，并尊重减少动态效果偏好。

#### 反例 / 踩坑
仅加 `loading="lazy"` 不会解决原图过大和解码峰值；虚拟列表用下标 key 会串图、串选中态。全局阻止 touchmove 会让纵向页面无法滚动，JS 跟手修改 top/left 还会反复布局。

#### 资深回答模板
「60–90 秒主回答：Q10/Q11 的分页和懒加载只是起点，我用录制区分响应式更新、DOM、图片解码和手势任务。数据分页或窗口化，状态按 ID 外置；图片按展示尺寸下发、占位并控制预览释放，手势先判方向再用 transform。最终在低端机、弱网和长时间滚动中看帧率与内存。」

#### 追问链
1. 聊天反向列表追加历史消息时，如何保持视觉锚点？
2. 图片预览为什么容易造成内存峰值，如何释放？
3. 虚拟列表中的曝光埋点如何避免重复计算？
:::

---

### D8. H5 使用 SSE 或流式 Fetch 时，重连、鉴权与 WebView 边界是什么？

::: details 参考答案
#### 基础结论
前面的弱网题不覆盖持续连接；D8 专注 SSE/流式 Fetch 的传输边界。原生 `EventSource` 适合 GET 单向推送，不能自定义 POST 与任意 Header；需要这些能力时使用流式 Fetch 或调整鉴权协议，并验证 WebView 支持。

#### 原理深挖
SSE 使用 `text/event-stream`，事件块可带 `id`、`event`、`data` 和 `retry`；浏览器会重连，可通过 Last-Event-ID 续传，但服务端必须保存或重放相应游标。HTTP/1.x 下同域并发连接数较低，HTTP/2 由流并发协商；代理缓冲、空闲超时和页面后台冻结都可能让「连接还在」成为假象。

#### 工程场景
协议定义 eventId、序号、心跳、终态和可重放窗口。原生 `EventSource` 默认会自动重连，并可读取服务端 `retry` 字段调整等待；若要自定义指数退避，必须在 `error` 后主动 `close()` 并自行重建连接，或由流式 Fetch 管理重试，不能一边保留原生自动重连一边再套定时器。鉴权优先同源安全 Cookie 或短时一次性票据；恢复时先对账再续流。

#### 反例 / 踩坑
把 SSE 当双向 WebSocket、每条消息都直接改大列表、无限立即重连，都会放大故障。给原生自动重连外再套指数退避会生成重复连接；依赖自定义 Authorization Header 的 EventSource 方案通常落不了地，只看 `onopen` 也不能证明代理没有缓冲消息。

#### 资深回答模板
「60–90 秒主回答：我先确认是单向事件流，再选 EventSource 或流式 Fetch。协议有事件 ID、心跳、终态和重放；EventSource 使用原生重连与服务端 `retry`，若要指数退避就主动关闭并重建，Fetch 则自行管理。恢复先对账，鉴权不用长期 query Token，并验证代理、HTTP 版本、后台冻结和 WebView 内核。」

#### 追问链
1. AI 流式输出中途断线，如何避免文本重复或缺段？
2. 服务端无法重放历史事件时，客户端恢复策略是什么？
3. iOS WebView 切后台后连接被冻结，回前台如何判活？
:::
